# 《游戏开发物语》界面设计规划

> 面向 `h5/` 现有实现的视觉与交互重构规格。
> 两条硬约束：**① 每屏内容一屏放完，不出滚动条；② 界面简洁、操作手感顺畅。**

> **现状注（2026-09-18）**：经营局已整体移除，sim 只剩生涯档。文中「招人 / 立项两步向导 / 公司档（`mode-company`）/ 破产结算」相关条目全部作废；「未做（下一轮）」第 1 条（立项向导）随之取消；落地记录里提到的 `h5/js/ui/panels.js` 已删除。其余生涯档条目继续有效。

---

## 0. 现状诊断

| 问题 | 现状 | 影响 |
|---|---|---|
| 主屏纵向堆叠超一屏 | `.scene-body` 为 `overflow-y: auto`，HQ 卡片逐个堆叠 | 小屏必出滚动条，违反约束 ① |
| 字号过小 | 最小 10px（kpi-lab、hint） | 可读性差，低于无障碍下限 |
| 信息密度未分级 | 主屏混入次要信息（同门工作室、MAU、广告说明） | 主次不清，撑高页面 |
| dock 二级按钮多 | L1 最多 8 个按钮 + 2 个 sheet 分组 | 误触率高、视觉噪音 |
| 反馈缺失 | 点「下一月」无过程反馈，进度条无过渡 | 手感生涩，违反约束 ② |

---

## 1. 布局系统（移动优先 · 一屏三区）

### 1.1 骨架

`#app` 改为 CSS Grid 三行，总高 `100dvh`：

```
grid-template-rows: auto minmax(0, 1fr) auto;
/* 行1 顶部状态区（固定，不滚）
   行2 内容区（自适应，唯一弹性区）
   行3 操作坞（固定，不滚） */
```

### 1.2 最小机型预算（iPhone SE · 375×667）

| 区 | 高度预算 | 内容 |
|---|---|---|
| 顶部状态区 | ≤ 88px | KPI 行（年月/积蓄/声望）48px + 规模/岗位/阶段 chips 28px + padding |
| 内容区 | ≥ 467px | 2×2 卡片网格（见 1.3） |
| 操作坞 | ≤ 96px | 常驻动作行 36px + 主按钮行 44px + padding（含 safe-area-inset-bottom） |

验证顺序：**320 → 375 → 390 → 430**，375 以下允许字号降档（clamp 兜底），不允许出现滚动条。

### 1.3 各屏信息架构

| 屏 | 一屏策略 |
|---|---|
| HQ 主界面 | **只保留 4 张卡**：角色（含四维）/ 在研（含进度 + 四维四宫格）/ 制作组（成员列表）/ 情报。`grid: 1fr 1fr / 1fr 1fr`，卡片内部 `overflow: hidden`。**在研卡属性**：四维各占一格的 2×2 四宫格（标签 + 数值 + bg-700 底色块），不放单行挤压式「需 程86…」；**制作组卡**：成员按行展示——头像圈（姓氏，描边用成员维度色）+ 姓名 + 右对齐职能，空位为虚线圈 +。**维度色全站统一**：程序=gold #e8a54b、策划=green #3ecf8e、美术=blue #6aa6d8、音乐=amber #c9873a，属性数值、头像描边、职能标签共用 |
| 招人 / 已发售 / 榜单 | 页面骨架（标题+分类 chips）固定不滚，**仅列表本体滚动**且隐藏滚动条；列表项做高度分页（每页 5 条 + 「更多」） |
| 立项 | 内容最多，改为**两步向导**：Step1 选题材+玩法+平台；Step2 填名+选周期+勾制作组。每步一屏，底部「上一步/下一步」 |
| 媒体评分 / 年度奖 | 骨架固定 + 结果区内滚 |
| 结算 / 破产 | 天然一屏，仅做排版收紧 |

### 1.4 防滚动五原则

1. **骨架三区固定**：只有内容区自适应，顶部/底部永不让位。
2. **密度分级**：每屏卡片 ≤ 4 张；次要信息（MAU、同门工作室、公式说明）收进底部弹层（bottom sheet），不占主屏。
3. **截断规则**：hint ≤ 2 行省略号；数字缩写（`1.2万` 不写 `12000`）；人名/项目名单行 ellipsis。
4. **长列表内滚**：列表页只有列表本体滚，`scrollbar-width: none`，分类用横向 chips 切换而不是纵向铺开。
5. **流式兜底**：`font-size: clamp(13px, 3.4vw, 14px)`；高度用 `100dvh`；`env(safe-area-inset-*)` 全保留。

### 1.5 断点

| 断点 | 行为 |
|---|---|
| ≤ 430px | 主体验证区间（单列卡片 2×2），App 内 WebView 主战场 |
| ≥ 576px | 内容区卡片可升 2×2→1×4 横排；KPI 加大到 18px |
| ≥ 992px | 桌面：居中 430px 舞台 + bg-950 留白，不拉伸 |

---

## 2. 配色系统（token 化）

延续现有暗紫 + 金的基调（视觉资产零成本迁移），系统化为令牌：

```css
:root {
  /* 背景四级 */
  --bg-950: #0c0a10;  /* 页面外底 */
  --bg-900: #16121c;  /* 画布 */
  --bg-800: #241c2c;  /* 卡片 */
  --bg-700: #2e2538;  /* 浮层/输入/次按钮 */
  --line:    #453a52; /* 描边（原 #3d3348 提亮一档） */
  /* 文字 */
  --text:  #f3ece3;   /* 主文字，卡上 13:1 */
  --muted: #a89bb2;   /* 次文字，卡上 6.3:1 */
  /* 主色（金） */
  --gold-500: #e8a54b; --gold-600: #c9873a; --on-gold: #1a1320;
  /* 语义色 */
  --green: #3ecf8e;  /* 收入/正向 · 卡上 8.2:1 */
  --red:   #e85d4c;  /* 危险/破产 · 4.8:1（≥12px 可用） */
  --blue:  #6aa6d8;  /* 信息/媒体 · 6.3:1 */
}
```

**语义规则**：金 = 行动与时间（CTA、年月、进度）；绿 = 钱进账；红 = 出事；蓝 = 客观信息。
**约束**：涨跌、盈亏必须配 ▲▼ 或文字前缀，禁止仅靠颜色传达（WCAG 1.4.1）。全屏同时只允许一个金色实心按钮（「下一月」），保持行动焦点唯一。

---

## 3. 字体系统（双字体制）

### 3.1 字体栈

```css
/* 显示字体：ark-pixel（12px 变体，自托管 woff2 子集化）— 用于标题、KPI 数值、年月 */
--font-display: "Ark Pixel 12px", "PingFang SC", "Microsoft YaHei", sans-serif;
/* 正文字体：系统栈 — 所有长句、说明 */
--font-body: -apple-system, "PingFang SC", "HarmonyOS Sans SC", "MiSans",
             "Microsoft YaHei", sans-serif;
```

- 像素字体**只负责数字与短标签**（≤6 字），长句一律系统字体。
- 像素字体按 **12px 整数倍**使用（12 / 24 / 36），非整数倍会糊。
- 所有数值 `font-variant-numeric: tabular-nums` 等宽对齐（像素字体天然等宽）。
- @font-face 用 woff2 + 按用字子集化（常用汉字 + 数字 + 冒号点号），控制体积 < 300KB；加载期间 fallback 到系统栈不阻塞。

### 3.2 字号刻度（8px 基线）

| 级 | 字号 | 字体 | 用途 |
|---|---|---|---|
| D1 | 24px | ark-pixel 700 | 屏幕大标题（boot、结算） |
| D2 | 16px | ark-pixel 700 | KPI 数值、年月 |
| D3 | 12px | ark-pixel 500 | 卡片标签（「在研」「制作组」），letter-spacing .08em |
| H1 | 18px | 系统 600 | 项目名、人名 |
| B1 | clamp(13px, 3.4vw, 14px) | 系统 400 | 正文 |
| B2 | 12px | 系统 400 | hint、chips —— **最小字号下限，禁止再小** |

行高：正文 1.5，数值 1.2。字重只用 400 / 500 / 700 三档。

---

## 4. 间距与组件规范

- **间距**：8px 基线（4 / 8 / 12 / 16 / 24），卡片间距 8px，卡内 padding 12px。
- **卡片**：bg-800 + 1px line + 圆角 12px；标题 D3，主内容 H1/B1。
- **按钮**：主按钮 gold-500 底 / on-gold 字，高 **44px**；次按钮 bg-700 底 + line 描边，高 44px；chip 高 32px（次级，间距 ≥ 8px）。
- **触达面积**：所有可点元素 ≥ 44×44px（现有 dock 按钮偏小需加高）。
- **弹层（bottom sheet）**：bg-700，上滑入场，圆角顶部 16px，拖拽把手 36×4px。

## 5. 微交互与手感

| 交互 | 参数 |
|---|---|
| 按钮按下 | `scale(.96)`，180ms `cubic-bezier(.2,.8,.3,1)`；抬起回弹 220ms |
| 场景切换 | opacity 0→1 + translateY(8px→0)，220ms（替换现有直接 display 切换） |
| dock sheet | translateY(100%→0)，240ms，带 40% 遮罩淡入 |
| 「下一月」 | 三连反馈：① 年月数字翻动 ② 资金 delta 绿/红飘字上浮 600ms ③ 在研进度条 `scaleX` 过渡 300ms |
| 进度条 | 用 `transform: scaleX()` 动画，不用 width（避免 layout） |
| Toast | 底部操作坞上方 12px，1.8s 自动消失，fade+slide |

全站只动画 `transform` / `opacity`；统一包 `@media (prefers-reduced-motion: reduce)` 降级为纯 opacity。

## 6. 无障碍检查清单（WCAG 2.2 AA）

- [x] 全部文字对比度 ≥ 4.5:1（见 token 注释，最低 red 4.8:1）
- [ ] 焦点可见：`:focus-visible { outline: 2px solid var(--gold-500); outline-offset: 2px }`
- [ ] 对话框补 `role="dialog" aria-modal="true"`，Esc 可关
- [ ] 禁用态除 opacity 外加「不可点」文案提示
- [ ] 涨跌/盈亏配 ▲▼ 符号，不只靠颜色

## 7. 落地顺序建议

| 阶段 | 内容 | 风险 |
|---|---|---|
| P0 | 替换 `:root` token + `#app` 三区骨架 + 字号下限 12px | 低，纯 CSS |
| P1 | HQ 重构为 2×2 四卡 + 次要信息进弹层 | 中，动 HTML 结构 |
| P2 | 立项改两步向导 + 列表页骨架固定/内滚 | 中 |
| P3 | 微交互全套 + 「下一月」反馈 | 低 |
| P4 | ark-pixel 子集接入（D1–D3 三档） | 低，字体文件自托管 |

> **流程提醒**：当前 Colorbox 状态机处于 security-review 阶段，本文件仅为设计规格；改 `h5/` 代码需按流程回到 implement 阶段进行。

---

## 9. 落地记录（2026-09-16 二稿批复后实施）

### 已落地

| 项 | 文件 | 说明 |
|---|---|---|
| 色彩 token | `h5/css/app.css` | `--line` #3d3348→#453a52、`--muted` #a396ab→#a89bb2、新增 `--gold-press` / `--amber` |
| 字号下限 | `h5/css/app.css` | kpi 标签 10→11px；玩家页 hint / skill tier / 奖项名 10→11px |
| HQ 一屏网格 | `h5/index.html` + css | `#sc-hq` 改 `.hq-grid` 2×2；四张卡：角色 / 在研 / 制作组 / 情报；`hq-sums` 从底部行改为网格内单元（公司档竖排计数） |
| 在研四宫格 | `h5/index.html` | `#hq-live-grid` 由 `grid4` 改 `grid2`，四维各占一格 |
| 制作组成员列表 | `h5/js/ui/paint.js` | 生涯档（自己+同事，按 `role.stat` 取维度色）/ 公司档（按四维最大值取色，显示「程/剧/美/音 · Lv」，trait 入 title） |
| 维度色统一 | `h5/css/app.css` | `.dim-p/dim-d/dim-a/dim-m` = 金/绿/蓝/橙；用于属性数值、头像描边、职能标签 |
| 公司档布局 | `h5/css/app.css` | `#app.mode-company`：隐藏角色卡，制作组卡跨两行、情报+计数堆叠右列 |
| 其他页面 | `h5/index.html`、`h5/js/ui/panels.js` | 在研制作单四维加维度色；招人页候选人属性行改维度色 span |
| 主按钮统一 | `h5/css/app.css` | 「下一月」改金色实心（原橙色渐变+重阴影）；「推进到抉择」改金色描边次按钮 |
| 微交互 | `h5/css/app.css` | 按钮/胶囊/卡片按压 `scale(.96)` 160ms；进度条 width 过渡 300ms；场景切换 fade+6px 220ms；`prefers-reduced-motion` 全量降级 |

### 实测数据（Chrome 无头内核，390/375 两档高度）

| 指标 | 结果 |
|---|---|
| `#hq-body` 滚动溢出 | **0**（764px 与 571px 两种可用高度均如此） |
| 文档整体溢出 | **0** |
| HQ 四卡尺寸 | 201×297 / 201×201（等分，2×2） |
| 制作组成员行 | 生涯档 5 行（自己+4 同事）、公司档 5 行 |
| 四宫格维度格 | 4 |
| 各页 JS 报错 | 无（生涯：HQ/自身；经营：HQ/招人） |

复核方式可复现：`.workbuddy/ui-check/make_harness.py` 生成 harness → Chrome `--headless=new --dump-dom`（读 `#qa-report` 的 JSON）与 `--screenshot`。

### 未做（下一轮）

1. 立项页两步向导、长列表页骨架固定 + 分页（当前仍是内容自适应）。
2. ark-pixel 字体接入（D1–D3 三档，需自托管子集化 woff2）。
3. 「下一月」三连反馈动效（年月翻动 / 资金飘字 / 进度条缩放）。
4. 底部弹层（bottom sheet）替代当前 `dock-l2` 二级菜单。
5. 现金数字千分位格式化（`1200000` 仍为原始值，属既有表现）。
