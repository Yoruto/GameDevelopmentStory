# 需求确认（开发者原话）

开发者确认按《游戏开发物语》完整经营向做第一版（2015–2025），游戏公司原味，先出设计不先看雏形。本轮对架构三点的原话：

1. 默认自动记录进度
2. 默认不分享
3. 玩家自己写，默认是喵扑studio

据此落定：关掉再打开自动接着玩；生涯结算不发微信/微博；公司名/游戏名玩家自己写，公司名默认「喵扑studio」（可改）。

存储方式（云端还是本机）、全站排行仍待看过页面后再选，本轮不拍板。


# 长线运营改造（开发者原话，2026-09-16）

开发者要求：移除长线的运营逻辑，长线作都换成单机开发方式（只保留「长线运营」标记），但版本要拆开——《原神：空月之歌》这种每年只发一个。TGA 年度最佳长线运营奖依然存在。

四个口子的回答：

1. 规模门槛：**保留**（仍要中等公司起）
2. 版本来源：**玩家重新立项**（系统只做「每系列每年 ≤1 部」闸门）
3. 评奖口径：**当年长线运营发售版本的 4 维合计**——「比如原神 2.0，魔兽世界 3.0 都在同一年发售，按照综合属性比较」
4. 数据范围：**只铺标志性系列**（约 15–20 个），其余长线保持单条

据此落定：长线不再占维护人、没有月收与固定开支、不能关服；长线走与盒装同一条 lifecycle；版本是独立作品，命名《系列：版本名》，同系列每个自然年最多 1 部。


# 排行榜合并（开发者原话，2026-09-17）

开发者要求：取消目前游戏中的月活排行榜，改成**长线和单机游戏共用一个排行榜，排行榜显示 15 名**。

三个口子的回答：

1. 排序口径：**纯本月实销**——两类同一把尺、按本月销量排；长线只在行尾另显示月活
2. 停售判定：**跟着缩到 15 席**——榜单即生死线，接受经营档难度上升
3. 垫底填充游戏：**路线 C**——裁成 15 条且值域上移（7200→1800），生死线约 900→1800 份

据此落定：只保留一个榜（前 15 席，长线与单机同榜），排序一律按本月实销、不给长线加权，行尾标类型、长线行另显示月活（月活不参与排序）；`ranking.size` 与 `lifecycle.chartSize` 同为 15；`chartFillers` 裁成 15 条、值域 7200→1800；单列月活榜写入「已废不要复活」。仅影响经营档，生涯档不走掉榜停售。

开发者已被告知并接受的两个副作用：月活退化为纯展示值（长线要变强须另给平缓衰减曲线，本轮不做）；榜单从 10 行涨到 15 行需重新确认一屏放得下。

确认凭据：开发者回复「继续」。
