{
  "id": "colorbox-request-activity-submit-vote",
  "name": "提交投票",
  "description": "让用户对指定活动的分组和候选选项进行投票，用于搭建投票互动玩法。",
  "category": "互动与玩法",
  "categories": [
    "互动与玩法"
  ],
  "icon": "badge-check",
  "details": {
    "capabilities": [
      "提交投票：让用户对指定活动的分组和候选选项进行投票，用于搭建投票互动玩法。",
      "管理投票、答题、积分结算或排行榜等互动数据。",
      "提供互动结果流、名次查询与积分变更的基础能力。"
    ],
    "scenarios": [
      "答题、投票、竞猜、积分挑战等互动玩法。",
      "需要将互动结果沉淀为用户积分或榜单数据的场景。"
    ],
    "usageTips": [
      "将积分与投票设计为可追踪、可幂等的服务端操作。",
      "在页面上说明参与规则、生效时间与防刷机制。"
    ]
  },
  "content": "---\nname: request-activity-submitVote\ndescription: 让用户对指定活动的分组和候选选项进行投票，用于搭建投票互动玩法。\n---\n\n# Usage\n\n- JS Path: `window.ColorboxAI.request.activity.submitVote(params)`\n\n# Constraints\n\n- 请求接口数据必须使用 window.ColorboxAI.request 能力及其子能力发起，禁止使用 fetch、axios、XMLHttpRequest、第三方请求库或其它自定义请求方式。\n- 数据能力调用必须避免无条件循环、无限轮询和重复渲染触发；同一参数的返回结果应优先保存在组件状态中复用。\n- 列表/分页类接口连续自动翻页最多 5 页，必须在无下一页、返回空列表或达到页数上限时停止。\n- SDK 前端已启用请求保护：默认不限制单能力并发和分钟调用量，不启用相同请求在途合并或 GET 短缓存；同 URL 在 10000ms 内最多 20 次；同一页面同接口（去掉 query/hash 参数后相同即为同一接口）在 5000ms 内最多 20 次，首次超过限制返回 429，后续同窗口请求静默丢弃，且同一窗口内只上报一次异常日志。不要自行循环重试或重复触发请求，若接口真实返回 408、429 或 503 等错误码，必须停止继续请求并给用户友好提示。\n- 必须根据返回的 code 判断是否成功，若 code 不等于 200，必须使用返回的 message 对用户进行友好提示（如 Toast 或弹窗）。\n\n# Parameters\n\n| 参数名 | 类型 | 是否必填 | 说明 |\n| --- | --- | --- | --- |\n| activityId | number | 是 | 投票活动唯一ID，例如 1 |\n| groupId | number | 是 | 投票分组唯一ID，例如 5 |\n| itemId | number | 是 | 候选项唯一ID，例如 14 |\n| voteNum | number | 否 | 投票票数，默认为 1 |\n\n# Returns\n\n- Type: `Promise<Response>`\n\n### Response Properties\n\n| 属性名 | 类型 | 是否必填 | 说明 |\n| --- | --- | --- | --- |\n| code | number | 是 | 状态码。200 表示成功，401 表示未登录，404 表示内容不存在，500 表示服务器错误 |\n| message | string | 是 | 错误提示信息。当 code 不等于 200 时必须用于向用户友好提示 |\n| data | object | 否 | 成功时返回的数据载荷 |\n| data.succeed | boolean | 否 | 是否投票成功 |\n\n# Examples\n\n```javascript\nwindow.ColorboxAI.request.activity.submitVote({ activityId: 1, groupId: 5, itemId: 14, voteNum: 1 }).then(res => { if (res.code === 200) { console.log(\"投票成功\"); } })\n```\n\n# Frontend Request Guard\n\n| 策略 | 默认值 |\n| --- | --- |\n| 同请求在途合并窗口 | 不启用（0ms） |\n| GET 缓存 TTL | 不启用（0ms） |\n| 请求超时 | 8000ms |\n| 单能力最大并发 | 不限制 |\n| 单能力分钟调用上限 | 不限制 |\n| 同 URL 窗口调用上限 | 20 次 / 10000ms（超过限制排队等待） |\n| 同页面同接口窗口调用上限（去掉 query/hash 参数） | 20 次 / 5000ms（首次超过限制返回 429，后续同窗口请求静默丢弃，异常日志只上报一次） |\n| 连续失败熔断 | 3 次失败后熔断 30000ms |\n| 自动分页建议上限 | 5 页 |",
  "files": [
    {
      "path": "SKILL.md",
      "content": "---\nname: request-activity-submitVote\ndescription: 让用户对指定活动的分组和候选选项进行投票，用于搭建投票互动玩法。\n---\n\n# Usage\n\n- JS Path: `window.ColorboxAI.request.activity.submitVote(params)`\n\n# Constraints\n\n- 请求接口数据必须使用 window.ColorboxAI.request 能力及其子能力发起，禁止使用 fetch、axios、XMLHttpRequest、第三方请求库或其它自定义请求方式。\n- 数据能力调用必须避免无条件循环、无限轮询和重复渲染触发；同一参数的返回结果应优先保存在组件状态中复用。\n- 列表/分页类接口连续自动翻页最多 5 页，必须在无下一页、返回空列表或达到页数上限时停止。\n- SDK 前端已启用请求保护：默认不限制单能力并发和分钟调用量，不启用相同请求在途合并或 GET 短缓存；同 URL 在 10000ms 内最多 20 次；同一页面同接口（去掉 query/hash 参数后相同即为同一接口）在 5000ms 内最多 20 次，首次超过限制返回 429，后续同窗口请求静默丢弃，且同一窗口内只上报一次异常日志。不要自行循环重试或重复触发请求，若接口真实返回 408、429 或 503 等错误码，必须停止继续请求并给用户友好提示。\n- 必须根据返回的 code 判断是否成功，若 code 不等于 200，必须使用返回的 message 对用户进行友好提示（如 Toast 或弹窗）。\n\n# Parameters\n\n| 参数名 | 类型 | 是否必填 | 说明 |\n| --- | --- | --- | --- |\n| activityId | number | 是 | 投票活动唯一ID，例如 1 |\n| groupId | number | 是 | 投票分组唯一ID，例如 5 |\n| itemId | number | 是 | 候选项唯一ID，例如 14 |\n| voteNum | number | 否 | 投票票数，默认为 1 |\n\n# Returns\n\n- Type: `Promise<Response>`\n\n### Response Properties\n\n| 属性名 | 类型 | 是否必填 | 说明 |\n| --- | --- | --- | --- |\n| code | number | 是 | 状态码。200 表示成功，401 表示未登录，404 表示内容不存在，500 表示服务器错误 |\n| message | string | 是 | 错误提示信息。当 code 不等于 200 时必须用于向用户友好提示 |\n| data | object | 否 | 成功时返回的数据载荷 |\n| data.succeed | boolean | 否 | 是否投票成功 |\n\n# Examples\n\n```javascript\nwindow.ColorboxAI.request.activity.submitVote({ activityId: 1, groupId: 5, itemId: 14, voteNum: 1 }).then(res => { if (res.code === 200) { console.log(\"投票成功\"); } })\n```\n\n# Frontend Request Guard\n\n| 策略 | 默认值 |\n| --- | --- |\n| 同请求在途合并窗口 | 不启用（0ms） |\n| GET 缓存 TTL | 不启用（0ms） |\n| 请求超时 | 8000ms |\n| 单能力最大并发 | 不限制 |\n| 单能力分钟调用上限 | 不限制 |\n| 同 URL 窗口调用上限 | 20 次 / 10000ms（超过限制排队等待） |\n| 同页面同接口窗口调用上限（去掉 query/hash 参数） | 20 次 / 5000ms（首次超过限制返回 429，后续同窗口请求静默丢弃，异常日志只上报一次） |\n| 连续失败熔断 | 3 次失败后熔断 30000ms |\n| 自动分页建议上限 | 5 页 |"
    },
    {
      "path": "schema.json",
      "content": "{\n  \"name\": \"request-activity-submitVote\",\n  \"description\": \"提交互动投票，支持对指定的活动、分组以及候选项进行投票。\",\n  \"inputSchema\": {\n    \"type\": \"object\",\n    \"properties\": {\n      \"activityId\": {\n        \"type\": \"number\",\n        \"description\": \"投票活动唯一ID，例如 1\"\n      },\n      \"groupId\": {\n        \"type\": \"number\",\n        \"description\": \"投票分组唯一ID，例如 5\"\n      },\n      \"itemId\": {\n        \"type\": \"number\",\n        \"description\": \"候选项唯一ID，例如 14\"\n      },\n      \"voteNum\": {\n        \"type\": \"number\",\n        \"description\": \"投票票数，默认为 1\"\n      }\n    },\n    \"required\": [\n      \"activityId\",\n      \"groupId\",\n      \"itemId\"\n    ]\n  },\n  \"outputSchema\": {\n    \"type\": \"object\",\n    \"properties\": {\n      \"code\": {\n        \"type\": \"number\",\n        \"description\": \"状态码。200 表示成功，401 表示未登录，404 表示内容不存在，500 表示服务器错误\"\n      },\n      \"message\": {\n        \"type\": \"string\",\n        \"description\": \"错误提示信息。当 code 不等于 200 时必须用于向用户友好提示\"\n      },\n      \"data\": {\n        \"type\": \"object\",\n        \"description\": \"成功时返回的数据载荷\",\n        \"properties\": {\n          \"succeed\": {\n            \"type\": \"boolean\",\n            \"description\": \"是否投票成功\"\n          }\n        }\n      }\n    },\n    \"required\": [\n      \"code\",\n      \"message\"\n    ]\n  }\n}"
    }
  ],
  "manifest": {
    "capability": "request-activity-submitVote",
    "source": "skill-package",
    "version": 1
  }
}
