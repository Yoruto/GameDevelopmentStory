# 玩法重设计方案（v2 草案）

> 状态：方向已与开发者确认，待细化拆任务
> 结论日期：2026-09-20
> 交付定位：从「逐月模拟经营」改造为「节点驱动的叙事生涯体验」，10-20 分钟一周目

---

## 0. 版本规则（长期有效）

- **只有明确的 res 版本才考虑存档兼容问题。**
- 当前所有版本均为 dev 版本，任何修改一律不考虑旧档兼容。
- 本次重设计为**大改**，直接开新档，不迁移存档。

## 1. 设计目标与约束

| 项 | 目标 |
|---|---|
| 一周目时长 | 10~20 分钟 |
| 玩家参与制作的游戏数 | 8~15 部（生涯总量） |
| 时间跨度 | 1995→2025，30 年 |
| 玩家操作密度 | 低：只做有分量的抉择，不看点数字 |
| 抉择影响链 | 选项 → 玩家属性（直接）→ 作品质量（间接）→ 荣誉 → 后续选项解锁 |
| 存档兼容 | 不考虑（dev 版本，见 §0） |

**核心循环（改后）**

> 「继续」→ 时间快进 → 停在下一个有分量的节点（抉择 / 事件 / 揭晓）→ 玩家做一次有意义的选择 → 再继续

---

## 2. 节点推进系统（替代过月）

### 2.1 机制

- 底层保留月 tick，但 UI 只暴露一个「**继续**」按钮 = 快进到下一个节点；中间月份的属性成长、开发进度**静默结算**。
- `sim.skipToNextNode()`：循环调用现有 monthly tick，直到命中节点判定。

### 2.2 节点类型（按优先级）

1. 剧情拍（eventLines，已有 waitUntil 调度，直接复用）
2. 带选项的开发事件（从 devEvents 106 条中筛出「有抉择分量」的子集）
3. 发售 / 评分揭晓日（四家媒体评语 + 均分 + 榜单演出）
4. 章末颁奖（见 §4）
5. 跳槽抉择（邀约 / 空窗 ≥ idleGap 阈值）
6. 晋升 / 时代里程碑

### 2.3 纯氛围事件降级

- 无选项的纯氛围事件不再逐个暂停，自动结算后进入「近况摘要」，在下一个节点前一次性呈现（每拍 ≤2 行文案）。

### 2.4 时间流逝感补偿

- 跨年 / 跨章时给年份大字幕 + 行业新闻条（1 行），避免快进导致"时间感丢失"。

### 2.5 节奏预算（验证用）

| 节点来源 | 数量估算 |
|---|---|
| 每部作品（立项抉择 + 1~2 开发事件 + 发售揭晓）≈4 节点 × 12 部 | ~48 |
| 章开场 | 6 |
| 年度颁奖 30 届（完整档 6~8 届 + 快讯档 22~24 届） | ~30 |
| 职业抉择（开局出身 + 3~4 次跳槽） | ~5 |
| 人物线拍（9 条线按章出没，含结局卡） | ~15 |
| **合计** | **~104 节点；快讯档 3~5 秒、抉择档 8~15 秒 ≈ 12~20 分钟** ✓（超时先砍快讯档与氛围事件） |

---

## 3. 抉择影响系统（重点）

### 3.1 影响链

```
玩家选择 ──直接──> 玩家属性（grantMainStatAndXp，已有）
玩家属性 ──矩阵──> 作品质量（personToTitle 换算，已有，唯一入口 sim.titleStatsFromPerson）
作品质量 ──评分──> 荣誉（TGA 提名/获奖 flag，已有）
属性 + 荣誉 + 职级 ──门禁──> 后续选项的可用性（新增）
```

### 3.2 选项门禁（新增 `req` 字段）

- 事件选项（devEvents / postLaunch / producerEvents / eventLines 拍的选项）支持：

```json
"req": { "minStat": { "program": 30 }, "flags": ["career.awardStorygoty"], "rank": 4 }
```

- 不满足的选项**置灰展示 + 一行解锁提示**（如"需要：程序 ≥ 30"），不隐藏。
- 目的：让玩家感知"属性和荣誉影响后续抉择"，同时制造多周目动机。
- ⚠️ 门禁判定必须 RNG-free（与 eventLines `skipIf` 同一纪律）。

### 3.3 收益叙事化

- 属性成长不再走裸结算面板，改为绑定在事件反馈文案上（例："主程离职，你被迫接手引擎层 → 程序 +2"）。
- 数值照旧走 `grantMainStatAndXp` / `sim.careerStatFactor`，只改呈现层。

---

## 4. 六章叙事骨架

> 章界贴合 eraStatScale 时代缩放；每章固定结构：
> **开场时代演出 → 职业抉择（去哪/做什么）→ 2~4 部作品经历 → 章末收束（时代谢幕 + 该年年终颁奖）**

| 章 | 年代 | 时代主题 | 玩家作品数 | 章内锚点作品（catalog 中已存在的高声望作） |
|---|---|---|---|---|
| 1 | 1995-1999 | 蛮荒萌芽：点阵、光盘与单机黄金期 | 1~2 | 仙剑奇侠传、命令与征服、网络创世纪、勇者斗恶龙VI |
| 2 | 2000-2004 | 网游浪潮：网吧时代与订阅制 | 2 | 暗黑破坏神II、博德之门II、最终幻想IX |
| 3 | 2005-2009 | 主机大战与画面军备竞赛 | 2 | 战神、使命召唤2、地下城与勇士、完美世界OL |
| 4 | 2010-2014 | 手游爆发：行业洗牌，TGA 诞生 | 2~3 | 文明V、战神3、尼尔 |
| 5 | 2015-2019 | 工业化 3A 与内卷 | 2~3 | 巫师3、斯普拉遁、奇迹暖暖、火箭联盟 |
| 6 | 2020-2025 | 次世代、开放世界与 AI 冲击 | 2 | 原神、哈迪斯、最后生还者 第二部、双人成行：分分合合 |
| | | | **合计 11~14** ✓ | |

### 4.1 章开场演出

- 年份大字幕 + 2~3 行时代白描（行业大事 + 生活细节，例：章 2 开场"街角的网吧贴出了通宵包夜的价目表"）。
- 素材可从各年代 landmark 作品的 `shipQuote` 风格延写。

### 4.2 年度颁奖（每年一届，双档制）

- **每年一届、全程 30 届**（1995-2013 为虚构「年度游戏大赏」，2014 起更名 TGA；复用现有 `awardStory{nominated,goty}` flag、滚幕 fx、四家媒体评语体系）。
- **快讯档**（玩家未参选/未提名的年份，约 22~24 届）：1~2 行快讯——当届年度游戏从该年 catalog 高声望作品中产生，附一行榜单。让玩家"看着行业史发生"，是体验 95-25 的重要一环。
- **完整档**（玩家作品参选的年份，约 6~8 届）：提名滚幕 → 获奖/落选演出 → 剧情拍。
- 快讯档 ≤5 秒、可连点略过；完整档不可跳。
- 若节奏超时，优先砍快讯档的榜单行（只留年度游戏一行）。

### 4.3 人物线编排

- 9 条 eventLines 按起止年代分配到章（mentor 线从章 1 起，junior 线不早于章 3，等——落地前先盘点 9 条线的年代覆盖）。
- 每章至少 2 条线有出没点，保证叙事连续感。
- ⚠️ 拍的 body 保持第一人称台词纪律（speakerBond 渲染规则不变）。

### 4.4 玩家路线

- 每章的职业抉择决定玩家参与哪些锚点作品：跳槽到不同公司 → 接触不同 catalog 作品。
- 一周目无法体验全部锚点 → 多周目差异（配合 §3.2 的置灰门禁提示）。

---

## 5. 厂商温和合并（128 → 约 70）

### 5.1 保留标准（温和版）

1. 作品数 ≥3 的公司（现有 57 家）全部保留；
2. 作品数 =2 且满足任一者保留：有 landmark 作品 / 被 openingOffer 或 eventLines 引用；
3. 预计保留 ~70 家；砍掉作品 ≤1 的 38 家 + 无 landmark 的双作品公司。

### 5.2 作品归并路径（按优先级）

1. `successorId` / `mergedYear`（字段已存在，直接用）；
2. 同 `publisherId` 的保留公司；
3. 同 `seriesId` 系列续作所在公司；
4. 兜底：同地区 + 同时代（hireFromYear 区间重叠）最近的保留公司。

### 5.3 同步清理清单

- 事件容器（devEvents / postLaunch / producerEvents / eventLines）中的公司引用；
- `openingOffer.tiers`（small/stable/wild 权重重配）、`offerMinRankByPower`、`hireChanceByPower`；
- 被砍公司的 `hireFromYear/hireUntilYear` 时间线缺口（保证每章可入职的公司 ≥5 家）。

---

## 6. 虚拟作品下线 → 游戏池顶班 + 开发周期拉长（✅ 2026-09-20 已落地，见 `REDESIGN-TASKS.md` 的 P2 / P2-fix）

### 6.1 虚拟池下线（✅ 已落地；其中的「空窗抉择」在 P2-fix 后降级为兜底，见 6.3）

- `virtualPool` 整块停用（字段保留、`enabled: false` 配置 gate，便于将来回滚）；
  所有生成路径 gate 在 `sim.virtualPoolEnabled` 之后。
- 空窗期改由节点推进吸收：
  - 空窗 < `idleGap.minDevMonths`(6)：快进直接跳过（P3 前为静默过月）；
  - 空窗 ≥6 月（或本公司已排不出下一档目录作）：出「空窗抉择」节点
    （接外包 `outsource` / 进修 `study` / 休息 `rest`），产出叙事化收益（属性小额、xp），**不再生成虚拟作品**。

### 6.2 真实作品周期拉长（✅ 已落地，`cycleMult = 1.75` 待原型验证）

- 周期系数 `cycleMult` 落地为 **1.75**（区间建议仍是 1.5~2.0，**待原型验证后收敛**）。
- 作品开发周期仍由体量（power 1/2/3）× 年代推导，以发售日倒排，保证发售月不变。
  唯一换算入口 `sim.titleDevStart`：`开工月 = 发售月 − round(原时长 × cycleMult)`。
- 效果：每部作品从"几个月的数值条"变成"一段有头有尾、中间有 1~2 个事件拍的经历"。

### 6.3 游戏池（P2-fix，✅ 2026-09-20 已落地）

- 原 `virtualPool` 改名并重新启用为 **`careerWorld.titlePool`**（游戏池）：**池里的作品不属于任何公司，
  谁缺作品就分给谁**。两个用途：**覆盖补全**（某公司某月排不出目录真作时抽一部顶上）与
  **入职/在岗兜底**（当月没活就直接抽，不再弹空窗抉择）。
- 覆盖保证（Master 2026-09-20 的决定）：每家公司从成立（或 1995 开局）到 2025 年底都要有活可干；
  老牌厂商允许几个月空窗。落地口径 `coverage.minFillMonths = 6` → **允许空窗 ≤ 5 个月**。
- 实测（`scripts/pool_coverage_report.py`）：P2-fix 当天 74 家 / 24216 公司-月，
  目录真作 42.1% + 池作 55.1% + 短空窗 2.9%（长空窗 5 月、超限 0 家）。
- 池作带 `virtual:true / pool:true` 标记：**不是目录真作**——不进 offer / 邀约准入、不改目录档期、
  不参与目录榜单候选；履历里显示为「过渡项目」。命名从题材名库抽、本局不重名。
- 生成是**确定性的**（哈希取模，不消费 RNG）：同一家公司同一个月永远抽出同一部作，重开档一致、读档不漂。
- 空窗抉择（6.1）保留为**池不可用时的兜底**（`titlePool.enabled = false` 时行为与 P2b 完全一致）。

### 6.4 池作占比配平：补数据 (a) + 补机制 (b)（P2-fix-a/b，✅ 2026-09-20）

P2-fix 落地后暴露的配平问题：**55% 的玩家时间是「过渡项目」**。拍板 a+b 一起做。

- **(a) 补数据** —— 先修口径（覆盖报告漏算 `hireUntilYear`，把消亡公司死后的月份当成空窗，
  真基线从 42%/55% 修正为 48%/48%）；再消灭 11 家「无接班的消亡公司」（2304 僵尸月）、
  删 7 组重复作、清 6 条 `worldFill_*` 占位作、修 2 处 `hireFromYear` 错配；
  主体是分三批补 **452 部真实历史作品**（523 → 975，含《国王密令II》《装甲核心》《战地1942》
  《半条命》《猴岛小英雄》《文明III》《巫师》《仁王》《死亡搁浅》类的公司代表作）。
  **结果：目录真作覆盖 48.2% → 80.3%，池作补全 48.5% → 17.0%**，最长空窗仍 5 月、超限公司 0 家。
- **(b) 补机制** —— 池作原来是一条惩罚线（成长砍半、晋升学分 4 折、不给奖项、履历只有四个字）。
  改为**中性偏好**：`xpPerVirtualRelease` 5→7、发售属性/职级经验倍率 0.5→0.75、作品学分 0.4→0.5、
  开发月 `devStatMult/devJobXpMult = 1.15`；叙事上按落盘分数档给一句专属评语（`copy.career.poolNotes`，
  与媒体评语同档），履历页标题拆成「署名作品 N · 过渡项目 M」。

> ⚠️ **连锁效应（P3 必须先拍板）**：属性成长的唯一来源是「在研月 × 阶段权重」，而 P2-fix 把空窗从
> 151 月（关池世界）压到 5 月 —— 等于凭空多出 146 个有成长的月份。整局实测（12 局 × 372 月，
> `scripts/_probe_career.js`）主职维终值：**关池 67.2 → b 之前 92.3 → b 之后 97.7**，
> 晋升硬门槛 60 的跨越时点从第 235 月提前到第 188 月。
> `jobRanks.statGain` 注释里那句「现在到 80 出头」是 P2 之前的实测，已不成立。
> 二选一（别一起改）：① `perDevMonth` 0.12→0.10；② 抬高 `promotion.requirements[].mainStat`。

---

## 7. 落地顺序（每步可独立验证）

| 步 | 内容 | 主要改动面 |
|---|---|---|
| ① | ✅ 厂商合并（纯数据清洗） | career-world.json + 事件引用 + 测试 |
| ② | ✅ 虚拟池下线 + 空窗抉择 + 周期拉长 | config + sim + 测试 |
| ②b | ✅ 游戏池顶班（覆盖补全 + 入职兜底，池作 = 原虚拟作机制复用） | career-world.json + sim + 测试 |
| ②c | ✅ 池作占比配平：补 452 部历史作 (a) + 池作收益与叙事 (b) | career-world.json + config + sim + ui + 测试 |
| ③ | 节点推进（skipToNextNode + 「继续」按钮 + 近况摘要） | sim + ui + 一致性测试 |
| ④ | 选项门禁 req + 置灰 UI + 收益叙事化 | config + ui |
| ⑤ | 六章编排（章容器、开场/章末演出、人物线分章盘点） | career-world.json + ui |

### 涉及文件

- `activity/career-world.json`：公司合并、作品归并、事件选项加 `req`、新增 `chapters` 容器
- `activity/*.json` → `python scripts/sync_config.py` → `h5/config.json` + `h5/js/config.generated.js`（不手改）
- `h5/js/sim.js`：skipToNextNode、虚拟作 gate、cycleMult
- `h5/js/ui.js`：「继续」按钮、章头/章尾演出、选项置灰、近况摘要
- `h5/js/careerLines.js`：拍调度复用，不改结构
- `tests/run-sim-tests.js`：新增 fast-forward 与逐月结算一致性断言、门禁 RNG-free 断言；现有 75 条中过月相关用例改写

---

## 8. 风险与验证

| 风险 | 对策 |
|---|---|
| fast-forward 与逐月结算不一致 | 两种驱动跑同一 tick 代码路径 + 测试断言结果一致 |
| 10-20 分钟装不下 80 节点 | 每拍文案 ≤2 行；近况摘要合并氛围事件；先做灰盒节点流跑时长 |
| 9 条人物线年代覆盖不全 | 落地前盘点各线起止年，缺章的线补拍或调年代 |
| 合并后某些年代可入职公司过少 | §5.3 清单里显式校验每章 ≥5 家 |
| 文案量大 | 分章分批写，先骨架后润色 |

**验证指标（灰盒即可测）**：一周目时长分布（目标 10~20min）、节点跳过率（玩家是否连点「继续」不看内容）、多周目差异感知（置灰解锁提示的触发率）。

---

## 9. 补充功能提案（多角度，不受旧架构约束）

### 9.1 推荐纳入 v2（按优先级）

| # | 功能 | 角度 | 设计要点 | 成本 |
|---|---|---|---|---|
| S1 | 生涯谢幕页 + 多结局 | 体验闭环 | 终局按「荣誉 × 人物 bond × 关键抉择」出 6~8 种结局卡；谢幕页 = 30 年作品年表 + 荣誉墙 + 人物线结局卡混剪。10-20 分钟的小游戏，结局是复玩的第一动力 | 中 |
| S2 | 开局出身三选一 | 玩家自主度 | 开局明选出身（小作坊 / 大厂稳定档 / 野路子），天赋同步改三选一——原「天赋三选一」从未实现，正好在重做开局时补上；出身决定第一章锚点作品池 | 低 |
| S3 | 行业大事年表 | 世界活性 | 每年 1 条行业快讯：从 catalog 当年 landmark 作品生成（大作发售 / 年度颁奖 / 公司合并走 successorId）。玩家自己做的 8~15 部之外，30 年行业史在眼前流过 | 低（与颁奖快讯档同源，纯数据复用） |
| S4 | 人物线结局卡 | 情感闭环 | 9 条线各有终局卡（bond/flag 驱动 2~3 变体：如 mentor 退休三态），谢幕页统一结算；玩家为"想看到某人好的结局"而重开 | 低 |
| S5 | 年代感视觉皮肤 | 沉浸 | 每章切换主色 / 点缀质感（像素→液晶→高清→次世代），CSS 变量即可；配合章开场年份大字幕强化"30 年"体感 | 低-中 |
| S6 | 荣誉墙 / 履历图鉴 | 收集欲 | 周目内随时可查：作品履历（四家评语 + 销量回溯，读 worldReleased.lifetimeSales 现有链路）、获奖记录、遇过的人 | 低 |

**S1 + S2 + S3 强烈推荐**：S3 与每年颁奖快讯档同机制，几乎零边际成本；S1/S2 补齐"开局有选择、结局有回收"两头，一周目的意义感才完整。

### 9.2 暂缓 backlog

| # | 功能 | 暂缓理由 |
|---|---|---|
| B1 | 多周目继承（NG+） | 二周目解锁新出身/天赋池；等一周目完成率与复玩数据验证后再做 |
| B2 | 自动模式 / 连点加速 | 等节点节奏调完（快讯档时长收敛后）再加，避免掩盖节奏问题 |
| B3 | 音效年代变迁 | 需要音频资源预算，纯加分项 |

### 9.3 落地并入

- S2 并入 §7 步骤⑤（章编排）一起做开局改造；
- S3/S4/S1 的数据结构（结局卡、快讯生成）并入步骤①的数据清洗（career-world.json 加 `endings` / 复用 `shipQuote`）；
- S5/S6 纯 UI，最后做。

---

## 10. 正反馈系统（2026-09-20 新增：三轴 + 补强）

> 已定三轴：①自身属性提升 ②作品获奖 ③作品销量/月活。
> 三轴覆盖了"我变强了 / 我被业界认可了 / 我的作品被市场认可了"，方向正确；以下是补强与补位建议。

### 10.1 三轴各自的补强

| 轴 | 频率 | 补强设计 |
|---|---|---|
| ① 属性提升 | 每节点（微反馈） | 叙事化结算（§3.3）之外，加**属性里程碑演出**：属性突破阈值（可对齐 promotion.requirements 的 28/34/41/51/60）触发"被世界承认"拍——主程让你主导引擎模块、制作人把下一部立项书交给你。属性是内在的，职责变化是它在外部的镜像，后者比 +2 有感得多 |
| ② 作品获奖 | 每年（大反馈） | 加 **build-up 悬念结构**：发售揭晓时四家媒体评语本身就是"离奖距离"的信号（表现力差评=明年奖项的短板预告）；落选年给"**有收获的落选**"——评语指路 + 回扣 req 置灰提示（"表现力再高 5 点，明年那席就是你的"），把落选变成目标提示而非纯挫败 |
| ③ 销量/月活 | 每作品（中反馈） | **具象化对比**：榜单名次 > 绝对数字；对标同期 landmark（"首月销量超过了同期的仙剑"）；月活走生活化描写（"网吧一半屏幕都是它"）。读现有 worldReleased/lifetimeSales 链路，纯呈现层改造 |

### 10.2 建议新增第四轴：声望（汇聚器）

- 三轴的汇出：作品 score / 奖项 / 销量累积 → **玩家声望等级**（显性称号链：无名新人 → 业界熟脸 → 中坚力量 → 明星制作人 → 时代之名）。
- 声望反哺玩法：①邀约档次（大厂主动来挖 = 地位反馈）；②并入选项门禁 `req`（新增 `renown` 键）；③结局分档（S1）。
- 作用：把三条零散的正反馈汇聚成**一条"我在行业里的位置在上升"的可感知曲线**，也补上"属性/奖项/销量到底换来了什么"的答案。
- 数值公式待设计验证（建议先按 获奖次数×权重 + 作品均分超阈值次数 累积，不做连续积分）。

### 10.3 建议新增第五轴：人的反馈（情感层）

- **人物线正反馈拍**：mentor 一句"你可以出师了"、peer 在颁奖礼起立鼓掌、junior 独立完成模块——9 条线显式编排正反馈节拍，与 §4.3 合并设计。
- **玩家的玩家反馈**：社区留言、梗图、同人创作（"有玩家把你的角色纹在了手臂上"）——"做游戏"题材独有的正反馈金矿，由 postLaunch 事件承载，量大管饱、成本低。

### 10.4 反馈节奏对齐表（防空档）

| 周期 | 反馈来源 |
|---|---|
| 每节点 | 属性微反馈（叙事化）、近况摘要 |
| 每作品 | 发售揭晓（四家评语 + 均分 + 榜单名次）、社区反应 |
| 每年 | 年度颁奖（完整档/快讯档）、声望称号变迁 |
| 每章 | 时代谢幕、人物线阶段收束 |
| 终局 | 谢幕页混剪（S1 多结局） |

- 防腻条款：保留温和挫折（落选、失败选项）做对比，但一律按"有收获的失败"处理（指路 + 可挽回）。
- 落地并入：声望字段并入步骤④（门禁）实现；人的反馈拍并入步骤⑤（章编排）；销量具象化并入步骤③（节点呈现层）。

---

## 11. 货币移除与替代属性（2026-09-20 新增）

### 11.1 结论

- **移除货币**：生涯档的「钱」整体是死配置（`livingCostMult` / `bondApartDelta` 早已禁用），且 10-20 分钟叙事化一周目里不存在消费决策点，保留只剩死 UI 与死文案。
- **替代属性定案：健康**（原提议「热情」已否——热情偏文艺抽象，健康有更硬的叙事事件与代价感）。

### 11.2 替代属性候选对比

| 候选 | 机制定位 | 主题契合 | 风险 |
|---|---|---|---|
| **健康（定案）** | 事件驱动增减；门禁 / 结局 / 人物线联动 | 30 年生涯最真实的代价轴：年轻通宵无事 → 中年扛不住；体检、住院、通宵全是现成叙事事件 | 若与作品质量耦合会搅浑 §3.1 影响链 → v2 先不耦合 |
| 热情 | 同上 | 初心母题贴叙事 | 抽象、难有具体事件演出，玩家不好感知"热情掉了"长什么样 |
| 精力 | 抉择代价资源 | 贴 crunch 题材 | 偏管理感，容易做成生存条，与"减少操作"冲突 |
| 人脉 | 影响邀约质量 | 贴行业现实 | 与 §10.2 声望轴信息重叠 |

### 11.3 健康设计

- **显示**：5 段体检式状态条、不显数字（符合"不看数字"原则），直接接管货币的 UI 位。
- **铁律：只随事件变动，绝不随时间自动衰减**——岁月感交给章末文案（"体检单上的箭头又多了两个"），不动数值，避免做成生存管理条。
- **恢复**：休息 / 进修类空窗抉择、规律作息类事件、家人与朋友线正反馈拍（§10.3）。
- **消耗**：赶工类抉择、连续大项目、压榨型事件（无良老板、砍项目连轴转）、失败发售后的消沉。
- **年代弧光**：同样 ±1 的赶工事件，年轻时文案轻描淡写、章 4 之后文案变重（"这次通宵后你缓了整整一周"）——数值规则不变，文案分年代，健康的故事感就出来了。
- **效果**：
  1. 选项门禁 `req` 加 `health` 键——**属性门"能不能"，健康门"扛不扛得住"**：低健康解锁"休整 / 降档 / 拒绝加班"分支，高健康解锁"连轴大项目"选项；
  2. 结局分档并入 S1（健康崩=提前退场结局：住院 / 转行离场；长青=时代之名结局）；
  3. 低于 2 段触发人物线关怀拍（家人的一次探望、junior 强行替你顶班、mentor 的"去休假，项目我盯着"）——与人的反馈轴天然联动。
- **v2 不做**"健康影响作品质量"（避免形成 健康→质量→荣誉→健康 的循环回路），是否耦合待原型验证。
- 数值：初始 4/5（年轻人身体好），事件 ±1，全程预计变动 10~15 次（待验证）。

### 11.4 移除清理清单

- state / UI 层货币字段与薪资类文案（offer 文案里若有钱数，改为平台与成长的叙词或直接删）；
- 死配置键 `livingCostMult` / `bondApartDelta`（天赋池与 `traitAxisDistinct` 同步收口）；
- `salaryMult` 若只喂薪资显示则一并清理（**动手前先核对引用**）；
- 相关守卫测试更新。

### 11.5 落地并入

- 货币移除 → §7 步骤②（配置清理时顺手做）；
- 健康字段、UI 条、门禁键 → 步骤④（与 req 门禁同批实现）。

---

*本方案为设计草稿，数值均为「待原型验证」区间；实施前需按 §7 顺序逐步过测试。*
